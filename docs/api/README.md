# docs/api

API endpoint catalog and examples, populated as services are implemented (Phases 4–12).

Live generated docs: springdoc-openapi Swagger UI per service through the gateway (`/api/<service>/swagger-ui.html`). This folder collects curated request/response examples and contract notes that complement the generated spec.
