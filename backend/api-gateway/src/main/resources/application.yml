server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/users/**
        - id: user-service-players
          uri: lb://user-service
          predicates:
            - Path=/api/players/**
        - id: sport-service
          uri: lb://sport-service
          predicates:
            - Path=/api/sports/**
        - id: sport-service-competitions
          uri: lb://sport-service
          predicates:
            - Path=/api/competitions/**
        - id: score-service
          uri: lb://score-service
          predicates:
            - Path=/api/scores/**
        - id: leaderboard-service
          uri: lb://leaderboard-service
          predicates:
            - Path=/api/leaderboard/**
        - id: leaderboard-service-s
          uri: lb://leaderboard-service
          predicates:
            - Path=/api/leaderboards/**
        - id: leaderboard-reports
          uri: lb://leaderboard-service
          predicates:
            - Path=/api/reports/**
        - id: leaderboard-websocket
          uri: lb:ws://leaderboard-service
          predicates:
            - Path=/ws/**

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
