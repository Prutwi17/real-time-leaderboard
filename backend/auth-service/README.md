# auth-service

Registration, login, BCrypt password hashing, JWT access tokens, rotating refresh tokens, USER/ADMIN roles (port 8081). Owns `auth_db` (`users`, `refresh_tokens`).

Implementation: Phase 4 ([IMPLEMENTATIONPLAN.md](../../IMPLEMENTATIONPLAN.md)) · Schema: [SCHEMA.md §3.1–3.2](../../SCHEMA.md) · Security: [SECURITY.md](../../SECURITY.md)
