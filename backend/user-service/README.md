# user-service

User profiles and player information (port 8082). Owns `user_db` (`user_profiles`); identity is referenced by `user_id` from validated JWT claims.

Implementation: Phase 5 ([IMPLEMENTATIONPLAN.md](../../IMPLEMENTATIONPLAN.md)) · Schema: [SCHEMA.md §3.3](../../SCHEMA.md)
