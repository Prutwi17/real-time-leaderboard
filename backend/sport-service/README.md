# sport-service

Configurable sports catalog (port 8083): list, admin CRUD, enable/disable. Owns `sport_db` (`sports`). Seed data: FOOTBALL, CRICKET, F1 — stored as rows, never hardcoded in business logic.

Implementation: Phase 6 ([IMPLEMENTATIONPLAN.md](../../IMPLEMENTATIONPLAN.md)) · Schema: [SCHEMA.md §3.4](../../SCHEMA.md)
