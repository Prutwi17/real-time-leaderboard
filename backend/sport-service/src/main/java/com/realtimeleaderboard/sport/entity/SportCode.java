package com.realtimeleaderboard.sport.entity;

/**
 * The complete, closed set of supported sports. Adding a sport later means
 * adding one enum value plus a seeded row - no database redesign.
 */
public enum SportCode {
    FOOTBALL,
    CRICKET,
    F1
}
